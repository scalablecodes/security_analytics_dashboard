# Pinned to the exact versions the model artefacts in models/ were trained with.
# The .joblib files are scikit-learn pickles and are not guaranteed to load under a
# different scikit-learn or numpy version, so do not loosen these without retraining.
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
imbalanced-learn==0.12.4
joblib==1.5.3
scipy==1.13.1
streamlit==1.50.0
plotly==6.9.0
